export function createDefaultDay(data: string) {
  return {
    'dateDay': data,
    'totalWorkingMinutes': 0,
    'allTimeInTimer': 0,
    'minutesOnPause': 0,
    'stops': 0,
    'tomatoes': 0,
  }
}